import { View, Text } from 'react-native';
import { IPlayComponentProps } from '@/types';

export function Play(props: IPlayComponentProps) {
  return (
    <View>
      <Text>TicTacToeCubed</Text>
    </View>
  );
}
